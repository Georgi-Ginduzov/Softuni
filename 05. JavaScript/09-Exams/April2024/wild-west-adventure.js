function solve(input){
    const numberOfPlayers = Number(input[0]);
    
    const charData = map

    for (let index = 0; index < numberOfPlayers; index++) {
        const characterData = input[index].split(' ');
        console.log(characterData);
    }

    let i = numberOfPlayers;
    while (input[i]) {
        
    }   
}

solve((["2",
"Gus 100 0",
"Walt 100 6",
"FireShot - Gus - Bandit",
"TakeHit - Gus - 100 - Bandit",
"Reload - Walt",
"Ride Off Into Sunset"]));